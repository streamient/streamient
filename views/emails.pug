extends layout

block content
	.kk-list-toolbar
		.d-flex.justify-content-between.align-items-center.mb-3
			h4 Emails

		if email_feature_enabled
			#batch-toolbar(data-type="emails")
				.form-check.mb-2
					input.form-check-input#select-all-cb(type="checkbox")
					label.form-check-label.small(for="select-all-cb") Select all
				#batch-actions.batch-floating-bar.d-none
					.batch-floating-inner
						.d-flex.align-items-center.gap-3
							span.batch-floating-count#batch-count 0 selected
							.d-flex.align-items-center.gap-2
								button.btn.btn-sm#batch-delete-btn
									!= icon('delete', 'me-1')
									| Delete
								button.btn.btn-sm#batch-move-btn
									!= icon('drive_file_move', 'me-1')
									| Move
								button.btn.btn-sm#batch-copy-btn
									!= icon('copy', 'me-1')
									| Copy
							span.batch-floating-banner.small.d-none#select-all-records-banner
						button.btn.btn-sm.batch-floating-close#batch-dismiss-btn(type="button")
							!= icon('close')

	if !email_feature_enabled
		.card.border-warning-subtle
			.card-body
				h5.mb-2
					!= icon('lock', 'me-2')
					| Email Ingestion (Pro)
				p.text-muted.mb-2 In hosted edition, email ingestion and email project views are available on the Pro plan.
				if is_hosted
					a.btn.btn-sm.btn-outline-warning(href="/settings/subscription") Upgrade to Pro
	else
		#emails-list.list-group
